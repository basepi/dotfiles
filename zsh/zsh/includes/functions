# Functions

function zsh_stats() {
  history | awk '{print $2}' | sort | uniq -c | sort -rn | head
}

function extract() {
  unset REMOVE_ARCHIVE

  if test "$1" = "-r"; then
      REMOVE=1
      shift
  fi
  if [[ -f $1 ]]; then
    case $1 in
      *.tar.bz2) tar xvjf $1;;
      *.tar.gz) tar xvzf $1;;
      *.tar.xz) tar xvJf $1;;
      *.tar.lzma) tar --lzma -xvf $1;;
      *.bz2) bunzip $1;;
      *.rar) unrar $1;;
      *.gz) gunzip $1;;
      *.tar) tar xvf $1;;
      *.tbz2) tar xvjf $1;;
      *.tgz) tar xvzf $1;;
      *.zip) unzip $1;;
      *.Z) uncompress $1;;
      *.7z) 7z x $1;;
      *) echo "'$1' cannot be extracted via >extract<";;
    esac

    if [[ $REMOVE_ARCHIVE -eq 1 ]]; then
        echo removing "$1";
        /bin/rm "$1";
    fi

  else
    echo "'$1' is not a valid file"
  fi
}

function sssh() {
    ssh cmyers@$1@scb.dmz.ut1.adobe.net
}
function s() {
    ssh cmyers@$1@scb.dmz.ut1.adobe.net
}

function scbscp() {
    sscp -S /Users/cmyers/src/work/scpagent.pl $1 cmyers@$2@scb.dmz.ut1.adobe.net:$3
}

function dev() {
    ssh cmyers@cstdev01.dmz.ut1@scb.dmz.ut1.adobe.net
}

function mom() {
    ssh cmyers@cst1.dmz.ut1@scb.dmz.ut1.adobe.net
}

function vpndown() {
    /opt/cisco/anyconnect/bin/vpn disconnect
    sudo iptables -D INPUT -d 45.33.35.16/32 -p tcp -m tcp --dport 22 -j ACCEPT
}

function vpnup() {
    printf 'cmyers\n'"$1"'\ny\n' | /opt/cisco/anyconnect/bin/vpn -s connect asa-lehi.adobe.com
    sudo iptables -I INPUT 1 -d 45.33.35.16 -p tcp --dport 22 -j ACCEPT
}
